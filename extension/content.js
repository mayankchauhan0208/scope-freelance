function visibleField(element){const style=getComputedStyle(element);return style.display!=='none'&&style.visibility!=='hidden'&&element.type!=='hidden'&&element.type!=='password'}
function fieldQuestion(element){const id=element.id;const label=id?document.querySelector(`label[for="${CSS.escape(id)}"]`):element.closest('label');return(label?.innerText||element.getAttribute('aria-label')||element.placeholder||element.name||'Unlabelled field').replace(/\s+/g,' ').trim()}
function scanForm(){return[...document.querySelectorAll('input, textarea, select')].filter(visibleField).map((el,index)=>({index,question:fieldQuestion(el),name:el.name||'',type:el.tagName==='SELECT'?'select':el.type||el.tagName.toLowerCase(),required:el.required,options:el.tagName==='SELECT'?[...el.options].map(o=>o.text.trim()).filter(Boolean):[]}))}
function fillDraft(answers){const fields=[...document.querySelectorAll('input, textarea, select')].filter(visibleField);let filled=0;for(const answer of answers){const el=fields[answer.index];if(!el||answer.value==null||['file','submit','button','checkbox','radio'].includes(el.type))continue;if(el.tagName==='SELECT'){const option=[...el.options].find(o=>o.value===answer.value||o.text.trim().toLowerCase()===String(answer.value).trim().toLowerCase());if(option)el.value=option.value;else continue}else el.value=answer.value;el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}));el.style.outline='2px solid #ff6b4a';filled++}return filled}
chrome.runtime.onMessage.addListener((message,_sender,sendResponse)=>{if(message.type==='SCOPE_SCAN')sendResponse({fields:scanForm(),url:location.href,title:document.title});if(message.type==='SCOPE_FILL')sendResponse({filled:fillDraft(message.answers||[])});return true});
